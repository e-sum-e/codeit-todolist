export enum ButtonType {
  SECONDARY = "secondary",
  DANGER = "danger",
  PRIMARY = "primary",
  SUCCESS = "success",
  DARK = "dark",
}

export type TodoType = {
  id: string;
  title: string;
  isDone: boolean;
};

export const seomiId = "e-sum-e";
